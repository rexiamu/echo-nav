# GitHub OAuth配置
# 请在GitHub创建OAuth应用后填入相应的值

# GitHub OAuth Client ID
VITE_GITHUB_CLIENT_ID=your_github_client_id_here

# GitHub OAuth Client Secret (注意：在生产环境中不应该暴露在前端)
VITE_GITHUB_CLIENT_SECRET=your_github_client_secret_here

# 应用版本
VITE_APP_VERSION=1.0.0

# 开发环境配置说明：
# 1. 访问 https://github.com/settings/applications/new
# 2. 创建新的OAuth应用：
#    - Application name: Echo Nav (或您喜欢的名称)
#    - Homepage URL: http://localhost:3000
#    - Authorization callback URL: http://localhost:3000/auth/github/callback
# 3. 创建应用后，复制Client ID和Client Secret
# 4. 将此文件复制为.env.local并填入真实的值
# 5. 重启开发服务器

# 生产环境配置说明：
# 1. 在GitHub创建生产环境的OAuth应用
# 2. Homepage URL: https://your-domain.com
# 3. Authorization callback URL: https://your-domain.com/auth/github/callback
# 4. 在Cloudflare Pages设置环境变量，不要将Client Secret提交到代码库

# Cloudflare Pages部署配置：
# 1. 在Cloudflare Pages项目设置中添加环境变量
# 2. 生产环境变量：
#    - VITE_GITHUB_CLIENT_ID: 生产环境的GitHub Client ID
#    - VITE_GITHUB_CLIENT_SECRET: 生产环境的GitHub Client Secret
#    - VITE_APP_VERSION: 应用版本号
# 3. 预览环境可以使用相同的变量或单独配置

# 推荐使用Personal Access Token：
# 本项目支持GitHub Personal Access Token认证，这是更安全简单的方式：
# 1. 用户访问 https://github.com/settings/tokens/new
# 2. 创建具有 'gist' 权限的token
# 3. 在应用中直接输入token进行认证
# 4. 无需配置OAuth应用和环境变量

# 注意事项：
# - 所有以 VITE_ 开头的变量会被打包到客户端代码中
# - 敏感信息在生产环境中应谨慎处理
# - Personal Access Token方式无需服务器端配置
